# SUNNY 개발 워크플로우 규칙

> 본 문서는 SUNNY 서비스 개발 시 Claude Code가 반드시 준수하는 작업 원칙이다.
> CLAUDE.md §12(Claude 행동 지침)와 함께 적용된다.
> 핵심 슬로건: **"바로 코딩하지 말고, 먼저 설계하라."**

---

## 0. 대원칙 — 기획과 구현의 분리

좋은 AI 코딩 = **기획(Thinking)과 구현(Writing)의 분리**.

```
[생각하기]  →  [사람의 검토 & 승인]  →  [작성하기]
 코드 이해         계획 검토·메모          계획대로 구현
 계획 수립         (아직 구현 X)           테스트 검증
```

- **계획을 사용자가 검토하고 승인하기 전까지 코드를 쓰지 않는다.**
- 모든 의미 있는 작업은 "코드 이해"에서 시작한다.
- AI의 역할은 단계에 따라 다르다: **계획 단계 = 설계 보조 / 구현 단계 = 실행자(설계자 X)**.

---

## 1. [1단계] 코드베이스를 먼저 깊게 읽는다 → research.md

구현 전, 관련 코드를 분석하고 결과를 `research.md`로 정리한다. **이 단계에서 구현 금지.**

확인해야 할 필수 체크리스트:
- 관련 파일·폴더
- 주요 함수·데이터 흐름
- 기존 API·컴포넌트
- 유사 기능 존재 여부 (중복 구현 방지)
- 위험한 부분
- 새 기능이 들어갈 위치

> 예시 프롬프트: "이 기능과 관련된 폴더와 파일을 깊이 읽고, 현재 구조가
> 어떻게 동작하는지 정리해줘. 구현은 하지 말고, 분석 결과를 research.md로 작성해줘."

---

## 2. [2단계] 구현 계획을 별도 문서로 작성한다 → plan.md

`research.md` 검토 후 바로 구현하지 않고, 먼저 `plan.md`를 작성한다. **아직 구현 금지.**

plan.md 필수 구성요소 (계획 문서 = 코드의 설계도):
1. **구현 목표** — 명확한 목표·비전
2. **변경할 파일 경로** — 대상 파일·디렉토리 식별
3. **파일별 수정 내용** — 구체적 코드 수정 사항 명세
4. **필요한 함수·컴포넌트** — 신규/수정 목록
5. **예상 코드 흐름** — 동작 시나리오·데이터 흐름
6. **테스트·검증 방법** — 기능·안정성 검증 계획
7. **기술 선택과 이유** — 적용 스택·선정 사유
8. **제외할 범위** — 이번에 다루지 않을 항목 명확화

> 예시 프롬프트: "research.md를 바탕으로 상세 구현 계획을 plan.md로 작성해줘.
> 수정할 파일, 변경 이유, 코드 흐름, 고려 사항을 포함해줘. 아직 구현하지 마."

---

## 3. [3단계] 사람이 계획을 검토하고 메모를 단다

AI의 계획은 **초안**, 최종 판단은 사람이 한다.

- 채팅창이 아니라 **문서(plan.md)의 정확한 위치에 메모**를 남긴다.
- AI와 사람이 같은 문서를 보고 협업한다.
- 메모 예시:
  - "이 API는 새로 만들지 말고 기존 API를 확장할 것"
  - "이 함수 시그니처는 외부 사용 중 — 변경 금지"
  - "캐싱은 이번 범위에서 제외"
  - "권한 검사는 서비스가 아니라 미들웨어에서"
  - "이 로직은 parent에서 이미 처리 — 중복 제거"
- 계획 단계 응답에는 **"아직 구현하지 마"를 명시**한다.

---

## 4. [4단계] 계획이 충분히 좋아질 때까지 반복한다

좋은 AI 코딩은 한 번에 끝나지 않는다.

```
① AI: plan.md 작성 → ② 사람: 검토 & 문서 메모 →
③ AI: 메모 반영 수정 → ④ 사람: 재검토 → (충분해질 때까지 반복) → 구현 시작
```

- 사람이 설계 방향을 계속 잡아간다.
- 계획이 충분히 좋아진 뒤에야 구현(Writing)을 시작한다.

---

## 5. [5단계] 승인된 계획대로 구현한다

계획 검토·승인이 완료된 후에만 구현을 지시·진행한다.

**구현 단계의 5가지 원칙:**
1. **즉석 설계 X** — 승인된 계획(plan.md)만 따른다.
2. **완료 작업은 문서에 표시** — 작업이 끝날 때마다 plan.md 체크리스트를 갱신한다.
3. **타입 체크·테스트 반복 실행** — 변경마다 검증한다.
4. **예상 외 문제 → 멈추고 보고** — 계획과 다른 상황이 나오면 임의 진행하지 않고 사람에게 보고·재승인.
5. **새로운 오류를 만들지 않는다** — 기존 통과 상태를 깨뜨리지 않는다.

> 이 단계 AI 역할 = **설계자(X) → 실행자(O)**

검증 명령 (프로젝트 package.json 기준):

| 목적 | 명령 |
|------|------|
| 타입체크 | `npx tsc --noEmit` |
| 빌드 검증 (타입체크 포함) | `npm run build` |
| 린트 | `npm run lint` |
| E2E 테스트 | `npm run test:e2e` (Playwright) |

- 기존에 통과하던 검증을 깨뜨리는 변경은 보고 후 작업을 중단한다.
- 실패한 검증 결과는 숨기지 않고 그대로 보고한다.

> 예시 프롬프트: "plan.md의 내용을 기준으로 전체 구현을 진행해줘. 각 작업이 끝나면
> 계획 문서에 완료 표시를 해줘. 타입 체크와 테스트를 계속 실행하면서 새로운 오류를
> 만들지 않도록 확인해줘."

---

## 6. 변경 최소화

- 요청 범위 밖의 리팩토링 · 정리 · 포맷 변경은 하지 않는다.
- 필요한 부분만 정밀하게 수정한다. (CLAUDE.md §12-3 연계)
