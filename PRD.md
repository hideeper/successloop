# SuccessLoop — PRD

> 상태: **계획 단계 (구현 시작 전)**. WORKFLOW.md 2~4단계에 해당.
> 본 문서를 검토하고, 수정할 부분에 직접 메모를 남겨주세요. 충분히 좋아지면 구현을 시작합니다.
> 원본 기획 참조: `process.hwp` (추출본: `_process_extracted.txt`)

---

## 1. 제품 개요

| 항목 | 내용 |
|------|------|
| 제품명 | **SuccessLoop** (확정) |
| 한 문장 | **"목표를 무의식에 각인시켜 뇌가 스스로 답을 찾게 만든다"** |
| 핵심 가치 | 올바른 목표 설정 + 정기적 리마인드 → 목표를 잠재의식에 프로그래밍 → 의식·무의식적 실현 행동 촉발 |
| 타겟 | 방향성 없이 노력만 하는 사람 / 열심히 하는데 성과가 없다고 느끼는 20~40대 |
| 형태 | 웹앱(PWA) 우선 → 검증 후 모바일 네이티브 확장 |
| 결정된 스택 | 신규 독립 스택 (아래 §3 권장안) |

### 작동 원리 (제품 철학 → 기능 매핑)

| 원리 | 제품 구현 |
|------|-----------|
| 목표를 명확히 정의하면 뇌의 **망상활성계(RAS)**가 무의식적으로 답을 수집 | 실현 목록·방향성 입력 + 반복 노출(리마인드) |
| 목표를 **반복적으로 각인** | 매일 아침/밤/일상 중 알림으로 실현 목록 재노출 |
| **진짜 목표** 발굴 | 하기 싫은 일 ↔ 하고 싶은 일 → 방향성(나침반) 도출 |
| 단기 좌절 방지 | SMART 단기목표로 장·단기 균형 |
| 자기 이미지 재프로그래밍 | (잠금 카테고리) 시각화·셀프 포지셔닝 |

---

## 2. 사용자 플로우 (전체 여정)

```
[온보딩] 회원가입(이메일 인증) → (선택)소셜 로그인(Google/Apple/Kakao) → PIN 설정
   │
   ▼
[1단계] 최초 1회 입력 (열람/수정/새로작성 가능, 새로작성 시 기존 버전 보존)
   인트로 카드 1~6 (교육·설득) 사이사이에 입력폼 배치
   ① 하기 싫은 일  ② 하고 싶은 일  ③ 인생의 나침반(방향성)  ④ 실현 목록(현재형)
   │  ← 1단계 완료 시 2단계 잠금 해제(히든→오픈)
   ▼
[2단계] SMART 단기목표
   매일 밤 10개 입력(레코드) → 그 중 '중요한 목표' 1개 선정
* 보완: 중요한 목표 선정하고 목표 달성을 위한 무엇이든 할 수 있는 사소한 행동 한가지 입력 
   │
   ▼
[일일 리마인드 루틴]  아침/일상중(랜덤)/밤 알림으로 반복
   │
   ▼
[Featured + 카테고리 홈] 성공목표 설정(1단계, 대표 카드+핵심가치) · 잠재의식 Daily on(2단계, SMART) · 시각화(잠금) · 셀프 포지셔닝(잠금)
```

---

## 3. 기술 방향 (권장안)

### 3-1. 스택

| 레이어 | 선택 | 이유 |
|--------|------|------|
| 프론트 | **Next.js 14 (App Router) + TypeScript + Tailwind** | 웹 우선, 추후 모바일은 동일 로직 재사용 용이 |
| PWA | Service Worker + Web App Manifest | 설치형 경험 + 웹 푸시 기반 |
| 백엔드/DB | **Supabase** (PostgreSQL + Auth + RLS + Storage + Edge Functions) | 신규 독립 스택, 인증·보안·스케줄러 일체형으로 개발 속도↑ |
| 인증 | Supabase Auth (이메일+비번, 이메일 인증) | 표준 제공 |
| 소셜 로그인 | **Google · Apple · Kakao** (Supabase 기본 제공) | 콘솔 키 발급 + 설정만으로 연동. 네이버는 일단 제외(추후 검토) |
| 알림 | **Web Push (VAPID)** + 서버 스케줄러(pg_cron → Edge Function) | 정시/랜덤/방해금지 서버 제어 |
| 배포 | Vercel(프론트) + Supabase Cloud | 관리형, 백업 내장 |
| 모바일(확장기) | **Expo (React Native) + Expo Notifications** | 동일 Supabase 백엔드 재사용, FCM/APNs를 래핑해 네이티브 푸시 확보 |

> **스택 확정 근거 (전략: 웹 검증 → 모바일 확장)**
> - Firebase(Next.js+Firestore) 대신 **Supabase 채택**: ① 이미 PostgreSQL(SUNNY) 사용 중 → 학습·유지보수 비용 최소 ② 버전 보존·정렬 목록·SMART 레코드 등 **관계형 데이터에 적합** ③ **RLS**로 사적 데이터 "본인 행만" 보안 ④ 비용 예측성·락인 회피.
> - Firebase의 결정적 우위(FCM 네이티브 푸시·Flutter)는 **웹 검증 단계에선 불필요** → 모바일 확장 시 **Expo 푸시**로 해결.

### 3-2. 알림 시스템 설계 (이 제품의 심장)

| 알림 | 트리거 | 구현 |
|------|--------|------|
| 매일 아침 정시 | 사용자 설정 시각 | pg_cron(1분 주기) → 해당 시각·타임존 사용자 조회 → Web Push |
| 일상 중 **랜덤** | 허용 시간대 내 무작위, **빈도(하루 N회)는 사용자 선택** | 서버가 사용자 설정 빈도(`random_per_day`)로 `next_random_at` 사전 계산·저장 → 도래 시 발송 후 재계산 |
| 매일 밤 정시 | 사용자 설정 시각 | 아침과 동일 |
| **방해금지(DND)** | 사용자 설정 구간 | 스케줄러가 DND 구간이면 발송 스킵 / 랜덤 계산에서 제외 |

> **랜덤 알림 UX**: 사용자가 랜덤 알림을 **켜면** 초기 기본값(하루 3회 · 09~21시)이 노출되고, **같은 설정 창에서 빈도·허용 시간대·방해금지 구간을 즉시 수정** 가능. 끄면 랜덤 발송 중지.

> ⚠️ **iOS 웹푸시 제약**: iOS 16.4+는 홈 화면에 설치한 PWA에서만 웹푸시 가능하고 신뢰성이 낮음.
> → **전략상 무리한 우회 불필요**: 웹 MVP는 고객 반응·사용성 **검증**이 목적이므로 Android/데스크톱 웹푸시 + 인앱 노출로 충분. iOS 알림 신뢰성은 **Expo 네이티브 확장 단계에서 해소**(Expo Notifications).

### 3-3. 데이터 모델 (개요 스케치)

```
profiles            (user_id PK, nickname, pin_hash, role, onboarding_step,
                     last_active_at, created_at)   # role: user|admin / onboarding_step: 입력 진행 깊이
roadmap_versions    (id, user_id, version_no, is_current, created_at)   # '새로작성 시 기존 보존'
dislikes            (id, user_id, version_id, content, sort)            # 하기 싫은 일
likes               (id, user_id, version_id, content, sort)            # 하고 싶은 일
compass             (id, user_id, version_id, content)                  # 인생의 나침반(방향성)
realizations        (id, user_id, version_id, content, sort)            # 실현 목록(현재형)
short_term_goals    (id, user_id, date, no(1~10), specific, measurable,
                     agreed, realistic, timely, is_important)           # SMART 10개/일
daily_actions       (id, user_id, date, content)                       # 사소한 행동
reminder_settings   (user_id PK, morning_at, night_at, random_enabled,
                     random_per_day, random_window_start, random_window_end,
                     dnd_start, dnd_end, timezone, next_random_at)   # random_per_day = 사용자 선택 빈도
push_subscriptions  (id, user_id, endpoint, p256dh, auth)
usage_events        (id, user_id, event_type, meta(jsonb), created_at)  # 퍼널·이탈·이용량 집계용
inquiries           (id, user_id, type, content, status, admin_reply,
                     replied_at, created_at)                            # 문의하기(§4-7), status: 접수|처리중|완료
```

- **보안**: 모든 테이블 Supabase **RLS**로 본인 행만 접근. 매우 사적인 데이터이므로 필수.
- `roadmap_versions`로 1단계 "새로 작성 시 기존 데이터 유지" 요구 충족.
- **관리자 분석**(§4-5): `usage_events` + `onboarding_step` + 집계 쿼리로 산출. `role='admin'`만 조회하며 **개인 콘텐츠(목표·하기싫은일 등)는 집계에서 제외** — 메타데이터·집계 지표만.

### 3-4. 디자인 톤 / 비주얼 가이드
> 톤: **퍼플 강조 + 화이트 elevated 카드 + 부드러운 그림자**의 깔끔·모던한 카드형 UI. 밝은 블루-그레이 배경에 카드가 살짝 떠 있는 무드.

- **컬러**
  - 주 강조(버튼·활성·진행): 퍼플 `#8B5CF6`
  - 보조 틴트(아이콘 타일·배지 배경): 연한 라벤더 `#F3E8FF` / 보더 `#E9D5FF`
  - 배경(페이지): 밝은 블루-그레이 `#F3F5F9` — 화이트 카드가 떠 보이게 / 카드: 화이트 / 텍스트: 딥 네이비-블랙 `#1A1A2E`
- **일러스트·아이콘**: 라인아트 + 퍼플 포인트. 카테고리 아이콘 = 라벤더 타일 + 라인 아이콘.
- **컴포넌트**: **화이트 elevated 카드**(둥근 모서리 16~20 + 부드러운 그림자로 살짝 떠 있음), **필(pill) 버튼**(퍼플 채움/아웃라인), **배지(pill 라벨)**, 아바타+상태 점, **넉넉한 여백**.
- **타이포**: 굵은 산세리프 헤드라인 + 가독성 본문.
- **하단 내비게이션**: 아이콘 탭, 활성 아이콘 퍼플.
- ※ 카테고리 타일은 **퍼플 단일 톤으로 통일**, 구분은 아이콘 형태로.

---

## 4. 기능 명세 (화면 단위)

### 4-홈. 메인 화면 — Featured + 카테고리 나열 (카드형 UI)
> 레퍼런스(상단 Featured 대표 카드 + 하단 카드 나열) 스타일로 구성.

**① 맨 상단 — Featured 대표 카드: 「성공목표 설정」 + 핵심 가치 문구**
- 메인 진입 시 가장 먼저 보이는 **기본/대표 항목**. 1단계(목표 설정)로 연결.
- 카드에 **서비스 핵심 가치 문구**를 함께 표기:
  - 제목: "나의 진정한 목표를 설정하세요"
  - **왼쪽 정렬**. 부제는 각 문장의 **앞 구절을 굵게 + 줄바꿈**:
    - **지속 가능성은** / 나로부터 나온 진실된 목표에서 시작됩니다.
    - **진정한 목표를** / 잠재의식에 새겨 나도 모르는 새 달성되는 목표를 경험하세요.
- 1단계 미완료 시 진행률(예: 2/4) 표시, 완료 시 "열기/수정".

**② 하단 — 카테고리 나열** (아이콘 + 제목 + 상태만, 설명 문구 없음)

| # | 카테고리 | 내용(내부 참조) | 상세 | 아이콘 | 상태 |
|---|----------|----------------|------|--------|------|
| 1 | **잠재의식 Daily on** <sub>(SMART 목표)</sub> | 2단계 SMART 10개/일 + 중요목표·사소한행동 | §4-2 | 체크리스트 `ti-checklist` | 1단계 완료 시 잠금 해제 |
| 2 | **시각화** | 미래의 성공한 나를 심상화 | §4-4 | 심상 `ti-eye` | 잠금(추가 서비스 암시) |
| 3 | **셀프 포지셔닝** | 슈퍼 셀프 이미지·정체성 (구 '직함 만들기') | §4-4 | 정체성 `ti-user-star` | 잠금(추가 서비스 암시) |

- **하단 카드엔 설명 문구 미노출** — 아이콘 + 제목 + 상태만. (핵심 가치 문구는 상단 Featured 카드에만)
- 카테고리 타일에 **실제 사진 썸네일** 적용(둥근 정사각, 톤 통일). **확정 이미지**: 잠재의식 Daily on=명상하는 인물+빛(잠재의식), 시각화=컬러풀한 3D 추상 오브제, 셀프 포지셔닝=화살표·열쇠구멍(방향·선택), 성공목표 설정=나침반/길.
- 카테고리명 **"잠재의식 Daily on"** (옆에 작게 "SMART 목표" 병기). 내부적으로는 2단계 SMART 단기목표. (이전 명칭: 단기 목표 → Daily 목표)
- 알림/리마인드("무의식 활성화")는 카드가 아니라 **백그라운드 알림 시스템**(§3-2) + 일일 루틴(§4-3)으로 동작.
- **1단계 미완료 시**: 상단 Featured 카드가 **셋업 진행 체크리스트(§4-1)** 역할(진행률 노출), 완료 후 하단 '잠재의식 Daily on' 잠금 해제.

### 4-내비. 하단 내비게이션 (4탭)
- **오늘**(기본 탭) · **목표** · **기록** · **설정**
  - 오늘 = 데일리 루프 허브(§4-오늘) / 목표 = 카테고리 그리드(§4-홈) / 기록 = 통계·캘린더(v1.1, MVP 최소) / 설정 = §4-설정

### 4-오늘. 오늘(Today) — 데일리 루프 허브 ⭐ (앱의 심장)
> 매일의 루프(읽기 → 떠올리기 → 입력 → 행동)를 한 화면에서 안내·수행. 알림을 놓쳐도 여기서 다 할 수 있어야 함.
- 인사(닉네임) + **연속 기록(streak)** 표시.
- (1단계 미완료 시) 성공목표 설정 진행 카드 우선 노출.
- **오늘의 루프 체크리스트**(완료 체크):
  - 아침 — 실현 목록 읽기 → §4-읽기
  - 일상 — 목표 떠올리기 (랜덤 알림 연동)
  - 밤 — 실현 목록 읽기 + 잠재의식 Daily on(SMART 10개) 입력
  - 사소한 행동 적기

### 4-읽기. 실현 목록 읽기 (아침/밤 루틴)
- 실현 목록을 큰 텍스트로 보여주며 읽게 함. 격려 문구: "입가에 웃음을 지으며 실현된 모습을 상상하며 읽어보세요."
- 일상 랜덤 진입 시: 최근 SMART 목표(없으면 실현 목록) + "목표를 달성하려면 어떻게 해야 하는가?" 문구.

### 4-잠금. PIN 잠금 (재진입·자동잠금)
- 앱 재진입/백그라운드 복귀(자동잠금 시간 경과) 시 **PIN 입력 잠금 화면**.
- 4자리 입력, (후순위)생체인증, "PIN을 잊으셨나요?" → 비밀번호 재인증으로 복구.

### 4-설정. 설정 허브
- 프로필(닉네임), PIN 변경, 알림 설정(§3-2 진입), 다크 모드, 데이터 내보내기(CSV·Excel), 문의하기(§4-7), 이용약관·개인정보 처리방침, 로그아웃, **회원 탈퇴(계정·데이터 완전 삭제)**.

### 4-0. 계정 / 보안
- 회원가입(이메일 + 이메일 인증), 로그인.
- **비밀번호 재설정/찾기** (이메일 링크).
- **소셜 로그인 연동**: Google · Apple · Kakao (Supabase 기본 제공). ※ 네이버는 Supabase 미지원이라 **일단 제외**(추후 검토). Apple은 iOS 앱 출시 시 필수.
- **로그인 화면 UI**: **풀스크린 배경 사진**(해를 향해 나아가는 이미지) + 가독성용 어두운 오버레이 + 상단 SuccessLoop 로고·태그라인 + 하단 화이트 폼 카드(이메일·비번·로그인 + 소셜 3종). ※ 다른 화면은 카드 무드(밝은 톤)이나 로그인만 몰입형 진입 화면.
- **PIN 번호**(4자리) 설정/입력 (재진입 시 잠금 해제).
  - **PIN 분실 복구**: 비밀번호 재인증으로 PIN 재설정.
  - **자동 잠금**: 백그라운드 전환·일정 시간 미사용 시 PIN 재요구.
- **회원 탈퇴 / 계정·데이터 완전 삭제** (개인정보보호법 대응, 신뢰).
- **가입 동의 (개인정보보호법 준수)**:
  - [필수] 만 14세 이상 확인 (14세 미만은 법정대리인 동의 필요)
  - [필수] 서비스 이용약관 동의
  - [필수] 개인정보 수집·이용 동의 (목적·항목·보유기간 명시)
  - [선택] 마케팅 정보 수신 동의 (이메일·푸시, opt-in)
  - **개인정보 처리방침은 '동의' 항목이 아니라 상시 공개**(앱 내 링크). 소셜로그인·클라우드(Supabase) 등 위탁·국외이전 사항을 처리방침에 고지.
  - UI: **"전체 동의하고 계속"** 버튼으로 필수+선택 일괄 동의 후 진행. 단, 개별 항목 토글 제공(선택 항목은 미동의로도 가입 가능해야 함).
  - ※ 목표 리마인드 알림은 서비스 기능이라 광고성 정보 동의 대상 아님. ※ **최종 약관·동의 문구는 법률/개인정보 전문가 검토 필수.**
- **데이터 내보내기** (사용자): 본인 로드맵 전체를 **CSV · Excel(.xlsx)**로 다운로드.
- **데이터·소스코드 자동 백업** (관리자/운영): 매일 자정 1회 자동 → 상세 §4-6.
- (후순위) 생체 인증(WebAuthn/네이티브), 2FA, 데이터 가져오기.

### 4-0b. 온보딩 / 사용성
- **첫 사용 가이드 투어** + **1단계 셋업 진행 체크리스트** (미완료 시 "어디까지 했는지" 우선 노출 → 상세 §4-1) — 카드 흐름 이탈 방지.
- **알림 권한 요청 온보딩** + **알림 테스트 발송** (권한 거부 시 핵심 기능 무력화 방지).
- **다크 모드** (매일 밤 사용 패턴), 글꼴 크기 등 기본 접근성.

### 4-0c. 검증 도구 (MVP 목표 = 사용성·반응 검증)
- **인앱 사용 이벤트 로깅** (`usage_events`) — 단계별 이탈·전환·이용량 측정. **외부 전송 없음**(프라이버시 우선, PostHog/GA 미사용). 집계·시각화는 §4-5 관리자 대시보드.
- **문의하기(고객 지원)** — 사용자 문의·피드백 수집. 상세 §4-7.

### 4-1. 1단계 (최초 1회 입력 · 열람/수정/새로작성)
> 규칙: 최초 1회 입력, 이후 **열람/수정/새로작성** 가능. 새로 작성 시 **기존 버전 보존**.

**셋업 진행 화면 (1단계 미완료 시 우선 노출)**
> 레퍼런스("Get set up for success" 진행 바 + 체크리스트)처럼, 1단계가 미완료면 진입 시 **"어떤 단계에 있는지·어디까지 완성했는지"를 먼저 제시**한다.
- 상단 **진행 바(4분할)** + 항목별 상태(완료 ✓ / 계속 →):
  ① 하기 싫은 일 · ② 하고 싶은 일 · ③ 인생의 나침반 · ④ 실현 목록
- 항목 탭 → 해당 폼으로 **이어쓰기**. 최상단에 "이어서 작성"(다음 미완료 항목으로 이동) CTA.
- 진행률은 `onboarding_step`/폼 완료 여부로 계산. **4개 모두 완료 시 1단계 클리어** → 카테고리 그리드 정상화 + 단기 목표 잠금 해제.
- 홈의 '목표 설정' 카드에도 진행률(예: 2/4) 배지를 표시.

인트로 카드(교육·설득 콘텐츠)와 입력폼이 번갈아 배치:

| 순서 | 유형 | 내용 |
|------|------|------|
| 카드1 | 인트로 | "성공하는 방법은 간단하다 — 목표를 적으면 실현된다" |
| 카드2 | 인트로 | 진짜 목표 vs 가짜 목표 |
| 카드3 | 인트로 | 하고 싶은 일을 찾기 위해 하기 싫은 일부터 명확히 |
| **폼A** | 입력 | **하기 싫은 일** — "직관적으로 떠오르는 모든 하기 싫은 일을 적어보세요", **[예시] 제공** |
| 카드4 | 인트로 | 하기싫음↔하고싶음 충돌 가능, 지금은 '내가' 하고 싶은 일 명확화 |
| **폼B** | 입력 | **하고 싶은 일** — 동일 안내, **[예시] 제공** |
| 카드5 | 인트로 | 방향성 = 인생의 나침반, 잠재의식을 깨운다 |
| **폼C** | 입력 | **인생의 나침반(방향성)** — 성장 과정·경험 회고, 솔직하게(고정 아님), [예시] 제공 |
| 카드6 | 인트로 | 뇌(망상활성계)가 무의식적으로 답을 찾는다, 목표는 많을수록 좋다 |
| **폼D** | 입력 | **실현 목록** (**현재형**), 개수 무제한, [예시] 제공 |
| 완료 | 게이트 | "1단계 클리어" → **2단계 잠금 해제** |

> **순서 주의** (process.hwp 갱신 반영): 폼B(하고 싶은 일)는 **카드4 다음**에 배치됨 (폼A → 카드4 → 폼B).
> **예시 제공 방식**: 예시를 상시 노출하지 않고, 폼에 **"예시가 필요하신가요?" 버튼** 배치 → 탭하면 **팝업(바텀시트/모달)** 으로 해당 폼의 예시를 보여줌. 예시 내용은 원본(`process.hwp`/추출본) 기준 — 하기싫은일 "브랜드 방향성과 맞지 않는 협업"…, 하고싶은일 "채무 없이 자연에 가까운 사옥 마련"…

**설명 카드 비주얼 — 실제 감각적 사진**
> 설명 카드 상단은 라인 아이콘 대신 **문구와 어울리는 실제 사진**(감성 무드)을 풀블리드로 배치. 카드별 **확정 사진**:
> - 카드1: 노트에 펜으로 글을 적는 손
> - 카드2: "THIS FEELS SO REAL" 텍스트 사인 (real = 진짜 목표)
> - 카드3: "OUT" 액자 사인 (빼낼 것·배제를 먼저)
> - 카드4: 노을 들판에 앉은 아이 뒷모습 (순수한 '진짜 나'의 바람)
> - 카드5: 별밤 아래 손에 든 나침반 (방향성)
> - 카드6: 빛이 비치는 터널 끝에 선 사람의 실루엣 (무의식이 답·길을 찾아냄)
> ※ 라이선스 확보한 스톡 또는 직접 촬영 사진 사용. 톤은 차분·자연광·따뜻한 무드로 통일. 실제 파일은 구현 단계에서 적용.

**입력 폼 비주얼 — 상단 영상 배너**
> 폼 A~D(및 Daily 목표) **상단에 무음 자동재생·루프 앰비언트 영상**을 배너로 배치 → 차분한 몰입감 조성.
> - 소리 기본 OFF(muted), 짧은 루프 클립, **지연 로드 + 압축본(mp4/webm)**.
> - 자체 호스팅(무료 스톡 Coverr/Mixkit/Pexels 또는 직접 제작), 외부 핫링크 지양.
> - 구분: **설명 카드 = 정적 사진 / 입력 폼 = 앰비언트 영상.** (가이드성 영상이 필요하면 "탭하면 재생" 방식으로 별도)

### 4-2. 2단계 = 잠재의식 Daily on (SMART 목표) — 1단계 완료 후 오픈
- 인트로: 장기 목표만으로는 단기 좌절 → SMART로 단·장기 균형.
- SMART 정의 안내: S(구체적) M(예측가능) A(동의) R(현실적) T(기일 명확) + 예시.
- **매일 밤 단기목표 10개 입력 폼**: No.1~10 레코드, 각 항목에 S/M/A/R/T 필드.
- 10개 중 **'중요한 목표' 1개 선정** (= 그것이 실현되면 다른 목표도 함께 실현되는 목표). 선택 기능 + 하단 작은 안내문구.
- **사소한 행동 1가지 입력** (보완): 중요한 목표 선정 후, 그 목표 달성을 위해 '무엇이든 할 수 있는 사소한 행동' 한 가지를 입력. (`daily_actions`에 저장 → 4-3의 6번 루틴과 연결)
- **예시 팝업**: "예시가 필요하신가요?" → ① **SMART 작성 예시**(목표 1개를 S·M·A·R·T로 전개) + ② **중요한 목표 선정 예시**(여러 목표 중 "실현되면 나머지도 함께 달성되는" 목표를 고르는 법)를 팝업(바텀시트)으로 제공.

### 4-3. 일일 리마인드 루틴 (안내 문구 포함)

| # | 시점 | 동작 | 안내 문구 |
|---|------|------|-----------|
| 1 | 매일 아침(정시 알림) | 실현 목록 읽기 | "입가에 웃음을 지으며 실현된 모습을 상상하며 읽어보세요" |
| 2 | 일상 중(정시/랜덤) | **최근 작성한 SMART 단기목표 10개** 띄움 (단기목표 입력 이력 없으면 **실현 목록**으로 폴백) | "목표를 달성하려면 어떻게 해야 하는가? 무엇이 필요할까?" |
| 3 | 매일 밤(정시 알림) | 실현 목록 읽기 | (1과 동일 문구) |
| 4 | 매일 밤(정시) | SMART 단기목표 10개 입력 | — |
| 5 | 매일 밤(정시) | 중요한 목표 설정 | — |
| 6 | (수시) | 사소한 행동 적기 | "생각나지 않으면 '답은 잠자는 동안 나온다'고 암시하고, 아침에 적어보세요" |

### 4-4. 잠금 카테고리 (UI 노출 / 내용 잠금 = 추가 서비스 암시)
1. **시각화** (`ti-eye`) — 잠들기 직전 미래의 성공한 나를 그려보기
2. **셀프 포지셔닝** (`ti-user-star`) — '슈퍼OOO' 슈퍼 셀프 이미지·정체성, 매일 아침저녁 확인 (구 '직함 만들기')

> MVP에서는 카테고리 카드만 노출하고 내용은 잠금 처리하여 "추가 서비스가 있음"을 암시.
> ※ "무의식 활성화"(알림·리마인드)는 잠금 카테고리가 아니라 핵심 알림 시스템(§3-2·§4-3)으로 구현됨.

### 4-5. 관리자 분석 대시보드 (관리자 전용 `/admin`)
> 접근 제어: `role='admin'`만. 라우트 미들웨어 + RLS 이중 차단.
> **프라이버시: 개인 콘텐츠(목표·하기싫은일·나침반 등) 비노출 — 집계·익명 지표만.**

**A. 가입자 분석**
- 총/신규 가입 추이(일·주·월), 가입 방식별(이메일 / Google / Apple / Kakao)
- DAU·WAU·MAU, 활성 vs 휴면, 탈퇴율, 리텐션 D1·D7·D30, 가입 코호트(주차별)

**B. 입력 진행 깊이 — "어디까지 입력했는지"**
- 사용자별·집계 현재 단계: 미시작 / 1단계 진행(폼 A→B→C→D 어디까지) / 1단계 완료 / 2단계 진행
- 폼별 작성 항목 수(실현목록·하기싫음·하고싶음 개수 분포)

**C. 이탈 지점 — "어디서 탈출하는지"**
- 온보딩 퍼널 단계별 이탈율 + 마지막 활동 단계(`onboarding_step`) 분포
- 가입 후 1단계 미시작률 / 1단계 완료 후 2단계 미진입률 / 일일 루틴 중단 지점

**D. 이용량 통계**
- 일·주·월 활성·세션, 입력 볼륨(일별 SMART 10개 입력 건수, 실현목록·사소한행동 작성 수)
- 알림 발송·열람·클릭 수, 기능별 사용 빈도, streak(연속일) 분포, 내보내기 사용률

**E. 추가 서비스 수요 신호**
- 히든 3종(무의식 활성화/시각화/직함) 클릭·관심도 → 유료화·확장 우선순위 근거

**F. 공통 필터/세그먼트**: 기간 · 가입 코호트 · 유입경로 · 플랫폼(웹/모바일) · 활성/휴면

### 4-6. 자동 백업 (관리자/운영)
- **주기**: **매일 자정(KST 00:00) 1회 자동.** 스케줄러로 실행(예: GitHub Actions cron / Supabase Edge Function cron / pg_cron).
- **① 데이터 백업**: 전체 DB 덤프(`pg_dump`) → 객체 스토리지(Supabase Storage / S3 등)에 저장. (Supabase 관리형 백업과 **별개의 자체 백업**)
- **② 소스코드 백업**: **Git 원격(GitHub 등)이 1차 백업.** 추가로 매일 자정 리포지토리 아카이브(또는 미러)를 스토리지에 보관(안전망).
- **보안 (필수)**: 백업물에 사용자 사적 데이터 포함 → **암호화 저장 + 접근 제한**(관리자/서비스 키만). 전송 구간 암호화.
- **보존**: 최근 N일 보관(예: 30일) 후 자동 삭제(비용·정리).
- **실패 알림**: 백업 실패 시 관리자에게 알림(이메일 등).

> 미정(구현 시 결정): 백업 스토리지 위치, 보존 기간(기본 30일 제안), 소스코드 야간 아카이브를 둘지(Git 원격으로 충분하면 생략 가능).

### 4-7. 문의하기 (고객 지원)
- **진입**: 설정/메뉴의 "문의하기".
- **제출**(로그인 사용자): 유형(버그 / 제안 / 계정 / 기타) + 내용(필수) + (선택)첨부. 등록 이메일 자동 연결.
- **저장**: `inquiries` 테이블, 상태 흐름 접수 → 처리중 → 완료.
- **관리자 처리**: `/admin`에서 문의 목록·상태 관리·답변. 신규 문의 시 관리자 알림(이메일 등).
- **답변(MVP)**: 등록 이메일로 회신. (인앱 스레드형 답변은 v1.1+)
- **프라이버시**: 문의 내용도 사적일 수 있음 → RLS로 **본인 문의만 조회**, 관리자만 전체 접근.

---

## 5. MVP 범위 / 단계별 로드맵

> **제품 전략: 웹으로 고객 반응·사용성을 검증한 뒤 모바일로 확장.** 따라서 MVP는 "빠른 출시·반복"에 최적화.

| 단계 | 범위 |
|------|------|
| **0. 설계 착수 전** | **린(lo-fi) 와이어프레임** 핵심 화면 4~6장(온보딩 카드 흐름 / 입력폼 4종 / 일일 리마인드 / 대시보드·잠금). 감성 인트로 카드만 시안 1~2장. ※ 풀 Figma 과투자 지양 — 콘텐츠·문구는 이미 확정 |
| **1. MVP (웹/PWA)** | 계정(비번 재설정)·PIN(분실복구·자동잠금)·회원탈퇴·약관 동의, 1단계 전체(카드+폼A~D, 버전 보존), 2단계, 일일 알림(정시+랜덤+DND, 웹푸시 + 권한 온보딩·테스트), 데이터 내보내기, 히든 카테고리 UI, 온보딩 가이드·진행률, 다크모드, **인앱 사용 이벤트 로깅 · 관리자 분석 대시보드(/admin) · 문의하기(고객 지원)**, **자동 백업(데이터+소스, 매일 자정)**. **목표 = 고객 반응·사용성 검증** |
| **2. v1.1** | 생체 인증·2FA, 데이터 가져오기, 관리자 수동 백업·복원 도구, 통계(연속 기록·캘린더 뷰), 공지사항 |
| **3. 모바일 확장** | **Expo(React Native) + Expo Notifications**로 네이티브 푸시(FCM/APNs) — iOS 알림 제약 해소, 동일 Supabase 백엔드 재사용 |
| **4. 추가 서비스** | 잠금 카테고리(시각화/셀프 포지셔닝) 콘텐츠 오픈. **현재는 전체 무료**, 유료화는 추후 검토 |
| **5. 커뮤니티 확장 (v2+)** | 목표 카테고리화 → 카테고리별 커뮤니티(익명/닉네임) + **목표 달성 과정 일지 게시판(열람·댓글·이모지)**. 옵트인 공유 모델 (상세 §8) |

---

## 6. 비기능 요구사항
- **프라이버시 최우선**: 목표·내면 데이터는 극히 사적 → RLS 강제, 전송 암호화, PIN 잠금, 최소 권한.
  - **관리자 분석도 집계·익명 지표만** — 개인 내면 기록(목표·하기싫은일 등)은 관리자도 열람 불가.
- 오프라인 열람(PWA 캐시) 고려.
- 다국어 확장 여지(글로벌 브랜드 비전) — i18n 구조 염두.

---

## 7. 검증 방법 (구현 단계에서)
- 1단계 입력→저장→새로작성 시 **기존 버전 보존** E2E 테스트.
- 1단계 완료 시 **2단계 잠금 해제** 동작 테스트.
- 알림 스케줄러: 정시 발송 / 랜덤이 DND 구간을 회피하는지 단위·통합 테스트.
- 타입체크·빌드·E2E를 변경마다 실행(WORKFLOW §5).

---

## 8. 향후 확장 비전 (v2+): 목표 카테고리화 & 커뮤니티

> 방향: 목표를 분석·**카테고리화** → 비슷한 목표를 가진 사람들의 **커뮤니티**(익명 또는 닉네임) + 카테고리별 **목표 달성 과정 일지 게시판**(열람·댓글·이모지).
> ⚠️ **MVP에서 구현하지 않음.** 아래는 비전과, 나중에 retrofit 비용을 줄이기 위해 지금 고려할 "기반"만 기술 (WORKFLOW §7 단순성 — 커뮤니티를 미리 짓지 않음).

### 8-1. 확장 기능 (v2+)
- **목표 카테고리화**: 사용자의 목표(실현목록·SMART)를 분석해 분류. 초기엔 수동 태그/규칙, 추후 AI·임베딩 클러스터링.
- **커뮤니티**: 같은 카테고리 사용자 그룹. **익명 핸들 또는 닉네임** 선택.
- **목표 달성 과정 일지 게시판**: 사용자가 특정 목표의 **달성 과정을 일지(다이어리)처럼 시간순으로 기록·공유**. 다른 유저는 이를 **열람 + 댓글 + 이모지 반응**으로 호응. (옵트인 공유물만, 사적 일기와 분리)

### 8-2. 지금 깔아둘 기반 (저비용 · retrofit 고통 방지)
- **프라이버시·공유 경계 (⭐ 가장 중요)**: 모든 개인 데이터는 **기본 비공개**. 커뮤니티 공유는 **명시적 옵트인**이며, 공유물은 원본 일기와 **분리된 별도 게시물 아티팩트**로 생성 — 1·2단계의 사적 기록이 자동 노출되는 일이 **절대** 없도록 설계.
- **익명 정체성**: 익명/실명 분리 **원칙만 지금 확정**. 필드(`profiles.anon_handle` + `community_opt_in` 기본 false)는 **v2에서 마이그레이션으로 추가**(저비용) — MVP 미반영.
- **카테고리 준비**: 목표는 이미 구조화된 레코드(`realizations`/`short_term_goals`) → 추후 `category_id`(nullable) + `categories` 체계를 붙일 수 있게 데이터 정합성 유지.
- **이벤트 확장성**: `usage_events`로 카테고리·커뮤니티 관심 신호도 추후 수집 가능.

### 8-3. 향후 데이터 모델 스케치 (v2, 참고용)
```
categories          (id, name, parent_id, sort)
goal_category       (goal_type, goal_id, category_id)        # 목표↔카테고리 매핑
process_journals    (id, author_id, category_id, goal_label, title,
                     is_anonymous, created_at)               # 한 목표의 '달성 과정 일지'(옵트인 공유물)
journal_entries     (id, journal_id, body, created_at)       # 시간순 일지 글(진행 기록)
comments            (id, entry_id, author_id, body, is_anonymous, created_at)  # 일지 글에 댓글
reactions           (id, entry_id, user_id, emoji)           # 이모지 반응(👍🔥👏…), (entry_id,user_id,emoji) UNIQUE
```
- `process_journals`/`journal_entries`/`comments`/`reactions`는 **사용자가 명시적으로 작성·공유한 콘텐츠만** 저장 — 1·2단계 사적 기록과 **물리적으로 분리**.
- 일지는 카테고리에 묶여 같은 목표를 가진 사람들이 서로의 **달성 과정을 읽고 댓글·이모지로 호응**.

---

### 결정 완료 ✅
- **전략**: 웹으로 고객 반응·사용성 검증 → 모바일 확장.
- **스택**: 웹 = Next.js PWA + Supabase / 모바일 확장 = Expo + Expo Notifications.
- **제품명**: **SuccessLoop** (확정).
- **소셜 로그인**: Google · Apple · Kakao (Supabase 기본 제공). **네이버 제외**(추후 검토).
- **과금**: **일단 전체 무료**, 유료화 추후 검토.
- **랜덤 알림**: 사용자가 **켜면** 초기 기본값(하루 3회 · 09~21시) 노출, **설정 창에서 빈도·시간대·DND 즉시 수정 가능**. 끄면 중지. (`random_per_day` 등)
- **데이터 내보내기**: **CSV · Excel(.xlsx)**.
- **관리자 분석**: 인앱 `/admin` 대시보드(자체 구축, **외부 전송 없음**), **MVP 포함**. 가입자 · 입력 진행 깊이 · 이탈 지점 · 이용량 등 집계 지표. **개인 콘텐츠 비노출**.
- **자동 백업**: 매일 **자정(KST) 1회 자동** — 데이터(`pg_dump`) + 소스코드(Git 원격 + 야간 아카이브) → **암호화 스토리지** 보관, 보존 N일. (상세 §4-6)

### 미해결 / 결정 필요
- 없음 — 핵심 결정 모두 완료.

---

> **아직 구현하지 않았습니다.** 위 내용 검토 후 수정·메모를 주시면 반영하고, 충분히 확정되면 구현을 시작하겠습니다.
